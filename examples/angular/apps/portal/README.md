# Portal Host

Runtime role:

- bootstrap the Angular application
- create UI execution contexts
- render the task board page
- connect UI actions to the backend API
- expose `packages/design_system` to the UI surfaces

Structural status:

- registry created
- Angular runtime entrypoint created
- task board shell renders through `src/root.component.ts`
- task UI Cases implemented and connected to the backend

Implemented screens:

- board page
- create task modal
