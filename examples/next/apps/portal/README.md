# Portal Host

Runtime role:

- bootstrap the Next.js portal runtime through the APP host bootstrap
- create UI execution contexts
- render the task board page
- connect UI actions to the backend API
- expose `packages/design_system` to the UI surfaces

Structural status:

- registry created
- Next.js App Router entrypoint created
- task board shell renders through `root.tsx`
- task UI Cases implemented and connected to the backend

Implemented screens:

- board page
- create task modal
