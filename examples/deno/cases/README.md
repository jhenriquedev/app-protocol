# Cases

This example uses one domain, `tasks`, with three atomic Cases:

- `task_create`
- `task_list`
- `task_move`

Each task Case exposes `domain`, `api`, `ui`, and `agentic`. No cross-case
composition is planned for v1.
