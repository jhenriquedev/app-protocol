# Data Package

Shared data access for the TypeScript example.

This package exposes the persistent board store used by the backend and agent
hosts. Cases consume it through `ctx.packages.data.boardStore`.
