# Design System Package

Server-rendered HTML primitives for the TypeScript portal host.

This package stays presentational. The `ui.case.ts` files decide when and how
to compose the primitives through `ctx.packages.designSystem`.
