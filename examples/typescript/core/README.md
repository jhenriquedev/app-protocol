# Core

This example keeps a local `core/` directory so the project preserves canonical
APP topology as a self-contained reference. The files here mirror the normative
APP contracts locally instead of re-exporting them from the repository root.
