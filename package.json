{
  "name": "app-protocol",
  "version": "0.0.10",
  "description": "AI-First Programming Protocol — specification and reference implementation",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:boundaries": "node scripts/validate-boundaries.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "npm run typecheck && npm run validate:boundaries",
    "skill:sync": "node scripts/sync-app-skill.mjs",
    "skill:pack": "npm pack ./tooling/skill-app",
    "release:notes": "node scripts/extract-changelog-section.mjs",
    "release:verify": "node scripts/verify-release-alignment.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
